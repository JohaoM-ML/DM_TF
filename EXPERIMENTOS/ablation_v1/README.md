# Ablación mapping v1

Comparación histórica del mapping v1 contra el pipeline canónico v2 (`OUTPUTS/`). El script que la
generó (`run_ablation.py`) dependía de módulos `SCRIPTS/*.py` retirados al consolidar el pipeline en
notebooks; se eliminó. Las salidas en `OUTPUTS/v1/` quedan como evidencia histórica de la comparación.

Conclusión: [`DOCUMENTACIÓN/reporte_mapping_v1_vs_v2.md`](../../DOCUMENTACIÓN/reporte_mapping_v1_vs_v2.md)
