# Ablación mapping v1

Compara el mapping histórico v1 contra el pipeline canónico v2 (`OUTPUTS/`).

```bash
python EXPERIMENTOS/ablation_v1/run_ablation.py
```

No modifica `OUTPUTS/` principal; escribe solo en `OUTPUTS/v1/` dentro de esta carpeta.
